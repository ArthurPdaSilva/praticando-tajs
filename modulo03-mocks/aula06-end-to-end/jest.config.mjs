/** @type {import('jest').Config} */
const config = {
  clearMocks: true,
  coverageProvider: "v8",
  //Analisar cobertura
  collectCoverage: true,
};

export default config;

// Statements → funções
// Branches → ifs
